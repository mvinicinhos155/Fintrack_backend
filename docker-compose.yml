version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: fintrack
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: fintrack
    ports:
      - "5432-5432"